"""
Launcher placeholder removed.

This file previously contained a Gradio demo. To keep the repository tidy and
to make `main.py` the single entrypoint, this launcher has been disabled.

Run the app with:

    python main.py

Do not restore this file unless you intend to re-enable the Gradio launcher.
"""